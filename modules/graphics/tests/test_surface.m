%=============================================================================
% Copyright (c) 2016-present Allan CORNET (Nelson)
%=============================================================================
% This file is part of Nelson.
%=============================================================================
% LICENCE_BLOCK_BEGIN
% SPDX-License-Identifier: LGPL-3.0-or-later
% LICENCE_BLOCK_END
%=============================================================================
% <--ADV-CLI MODE-->
%=============================================================================
f = figure();
[X, Y] = meshgrid(1:0.5:10,1:20);
Z = sin(X) + cos(Y);
h = surface(X, Y, Z);
%=============================================================================
f = figure();
ax = gca();
[X, Y] = meshgrid(1:0.5:10,1:20);
Z = sin(X) + cos(Y);
h = surface(ax, X, Y, Z);
%=============================================================================
f = figure();
[X,Y] = meshgrid(1:0.5:10,1:20);
Z = sin(X) + cos(Y);
C = X.*Y;
surface(X, Y, Z, C);
colorbar
view(3)
%=============================================================================
f = figure();
ax = gca();
[X, Y] = meshgrid(-5:.5:5);
Z = Y.*sin(X) - X.*cos(Y);
s = surface(X,Y,Z,'FaceAlpha',0.5, 'EdgeColor', 'none');
view(3)
%=============================================================================
f = figure();
N = 50;
[X,Y,Z] = peaks(N);
CO = [];
CO(:,:,1) = zeros(N);
CO(:,:,2) = ones(N).*linspace(0.5,0.6,N);
CO(:,:,3) = ones(N).*linspace(0,1,N);
h = surf(Z,CO);
%=============================================================================
