<?xml version="1.0" encoding="UTF-8" ?>
<xmldoc>
  <copyright>SAME AS NELSON SOFTWARE</copyright>
  <language>fr_FR</language>
  <keyword>arguments</keyword>
  <short_description>bloc de validation des arguments de fonction.</short_description>
  <syntax>
    <syntax_item>arguments, argSpecs, end</syntax_item>
    <syntax_item>arguments (Input), argSpecs, end</syntax_item>
    <syntax_item>arguments (Repeating), argSpecs, end</syntax_item>
    <syntax_item>arguments (Output), argSpecs, end</syntax_item>
    <syntax_item>arguments (Output,Repeating), argSpec, end</syntax_item>
  </syntax>
  <description>
    <p>
      <b>arguments ... end</b> déclare les arguments d&apos;entrée pour une fonction.
      Le bloc est optionnel.
      Si un ou plusieurs blocs <b>arguments</b> sont inclus, ils doivent tous apparaître avant la première ligne exécutable de la fonction.
      Un bloc sans qualificateur est traité comme un bloc d&apos;entrée.</p>
    <p>Chaque déclaration d&apos;argument suit cette forme :</p>
    <p>
      <tt>argName (dimensions) class {validators} = defaultValue</tt>
    </p>
    <p>
      <b>(dimensions)</b> — Taille de l&apos;entrée spécifiée sous forme de liste séparée par des virgules d&apos;entiers ou de deux-points,
      par exemple <tt>(1,1)</tt>, <tt>(1,:)</tt>, ou <tt>(3,5,2)</tt>.
      Un deux-points permet n&apos;importe quelle longueur dans cette dimension.
      L&apos;entrée doit correspondre exactement aux dimensions déclarées, ou être compatible avec elles
      (par exemple, un vecteur colonne est compatible avec <tt>(1,:)</tt> et est automatiquement redimensionné).
      Les expressions ne sont pas autorisées dans les dimensions.</p>
    <p>
      <b>class</b> — Un nom de classe unique tel que <tt>double</tt>, <tt>char</tt>, ou <tt>string</tt>.
      La valeur est convertie en cette classe lorsque cela est possible.
      Si omis, toute classe est acceptée.</p>
    <p>
      <b>{validators}</b> — Une liste séparée par des virgules de fonctions de validation, entourée d&apos;accolades ,
      par exemple <tt>{mustBeNumeric, mustBeReal}</tt>.
      Les fonctions de validation génèrent une erreur lorsque la condition n&apos;est pas remplie ; contrairement à la classe, elles ne modifient jamais la valeur de l&apos;argument.</p>
    <p>
      <b>= defaultValue</b> — Une expression qui fournit une valeur par défaut et rend l&apos;argument optionnel.
      L&apos;expression peut faire référence aux arguments déclarés précédemment.
      Les arguments optionnels doivent être positionnés après tous les arguments requis dans la signature de la fonction et dans le bloc <b>arguments</b>.</p>
    <p>
      <b>arguments (Repeating) ... end</b> déclare des arguments d&apos;entrée répétitifs.
      Une fonction ne peut contenir qu&apos;un seul bloc d&apos;entrée répétitif.
      Nelson crée un tableau de cellules pour chaque argument répétitif contenant toutes les valeurs passées pour cet argument.
      Si la fonction possède également des arguments nom-valeur, ceux-ci doivent être déclarés dans un bloc <b>arguments</b> séparé après le bloc répétitif.</p>
    <p>
      <b>arguments (Output) ... end</b> déclare des arguments de sortie.
      Les blocs de sortie doivent apparaître après tous les blocs d&apos;entrée mais avant la première ligne exécutable de la fonction.
      Lorsque des blocs d&apos;entrée et de sortie sont présents, il est recommandé d&apos;utiliser les qualificateurs explicites <b>(Input)</b> et <b>(Output)</b> pour une meilleure lisibilité.
      Les arguments de sortie ne peuvent pas définir de valeurs par défaut, et les fonctions de validation appliquées à un argument de sortie ne peuvent pas faire référence à d&apos;autres arguments de sortie.</p>
    <p>
      <b>arguments (Output,Repeating) ... end</b> déclare un seul argument de sortie répétitif.
      Au maximum un argument de sortie répétitif est autorisé par fonction.<tt>varargout</tt> peut apparaître dans un bloc de sortie répétitif uniquement lorsqu&apos;il est le seul argument de sortie.</p>
    <p>      Pour les arguments nom-valeur, utilisez la notation <tt>nv.name</tt> dans le bloc <b>arguments</b>,
      où <tt>nv</tt> correspond au nom de la structure utilisé dans la signature de la fonction.</p>
    <p>      Les blocs <b>arguments</b> ne peuvent pas être utilisés dans les fonctions imbriquées, les méthodes abstraites ou les méthodes destructrices de classes handle.</p>
  </description>
  <used_function />
  <bibliography />
  <examples>
    <example_item>
      <example_item_type>nelson</example_item_type>
      <example_item_description>Restreindre la taille et le type d&apos;un argument d&apos;entrée. Un vecteur colonne est accepté car il est compatible avec la taille (1,:) et est automatiquement redimensionné.</example_item_description>
      <example_item_data runnable="cli"><![CDATA[
function [m, s] = twoStats(x)
  arguments
    x (1,:) {mustBeNumeric}
  end
  m = mean(x, 'all');
  s = std(x, 1, 'all');
end
]]></example_item_data>
    </example_item>
    <example_item>
      <example_item_type>nelson</example_item_type>
      <example_item_description>Argument optionnel avec une valeur par défaut dérivée d&apos;un argument déclaré précédemment.</example_item_description>
      <example_item_data runnable="cli"><![CDATA[
function c = myMul(a, b, c)
  arguments
    a uint32
    b uint32
    c uint32 = a .* b
  end
end
]]></example_item_data>
    </example_item>
    <example_item>
      <example_item_type>nelson</example_item_type>
      <example_item_description>Utiliser des fonctions de validation pour restreindre les valeurs des arguments. L&apos;argument method est optionnel et par défaut vaut &apos;linear&apos;.</example_item_description>
      <example_item_data runnable="cli"><![CDATA[
function r = myInterp(x, method)
  arguments
    x (1,:) double {mustBeNumeric, mustBeReal}
    method (1,:) char {mustBeMember(method, {'linear', 'nearest'})} = 'linear'
  end
  r = {x, method};
end
]]></example_item_data>
    </example_item>
    <example_item>
      <example_item_type>nelson</example_item_type>
      <example_item_description>Déclarer des arguments nom-valeur optionnels en utilisant une structure. Les options LineStyle et LineWidth ont des valeurs par défaut, donc elles sont optionnelles.</example_item_description>
      <example_item_data runnable="cli"><![CDATA[
function myRectangle(X, Y, options)
  arguments
    X double
    Y double
    options.LineStyle (1,1) string = "-"
    options.LineWidth (1,1) {mustBeNumeric} = 1
  end
  % Function code
end
]]></example_item_data>
    </example_item>
    <example_item>
      <example_item_type>nelson</example_item_type>
      <example_item_description>Déclarer des arguments d&apos;entrée répétitifs. Nelson crée un tableau de cellules pour chaque argument répétitif.</example_item_description>
      <example_item_data runnable="cli"><![CDATA[
function fRepeat(x, y, style)
  arguments (Repeating)
    x (1,:) double
    y (1,:) double
    style {mustBeMember(style, {'--', ':'})}
  end
  z = reshape([x; y; style], 1, []);
  if ~isempty(z)
    plot(z{:});
  end
end
]]></example_item_data>
    </example_item>
    <example_item>
      <example_item_type>nelson</example_item_type>
      <example_item_description>Valider à la fois les arguments d&apos;entrée et de sortie en utilisant des blocs séparés.</example_item_description>
      <example_item_data runnable="cli"><![CDATA[
function out = myFunction(A, B, C)
  arguments (Input)
    A (1,1) string
    B (1,:) double
    C (2,2) cell
  end
  arguments (Output)
    out (1,:) double
  end
  out = B;
end
]]></example_item_data>
    </example_item>
    <example_item>
      <example_item_type>nelson</example_item_type>
      <example_item_description>Arguments d&apos;entrée et de sortie répétitifs avec validation. Restreint à la fois les entrées et les sorties aux vecteurs ligne.</example_item_description>
      <example_item_data runnable="cli"><![CDATA[
function vectorSum = repeatSum(a, b)
  arguments (Input,Repeating)
    a (1,:)
    b (1,:)
  end
  arguments (Output,Repeating)
    vectorSum (1,:)
  end
  n = numel(a);
  vectorSum{n} = a{n} + b{n};
  for i = 1:n-1
    vectorSum{i} = a{i} + b{i};
  end
end
]]></example_item_data>
    </example_item>
  </examples>
  <see_also>
    <see_also_item>
      <link linkend="${interpreter}function">function</link>
    </see_also_item>
    <see_also_item>
      <link linkend="${interpreter}iskeyword">iskeyword</link>
    </see_also_item>
  </see_also>
  <history>
    <history_item>
      <history_version>1.17.0</history_version>
      <history_description>initial version</history_description>
    </history_item>
  </history>
  <authors>
    <author_item>Allan CORNET</author_item>
  </authors>
</xmldoc>
